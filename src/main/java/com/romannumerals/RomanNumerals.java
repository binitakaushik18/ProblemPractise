package com.romannumerals;

/**
 * Created by binita on 05/03/15.
 * Create a class (RomanNumerals) with the following interface:
 String convert (int number);
 Given an Arabic numeral, return the corresponding Roman numeral (ex. 2 >> “II”, 10 >> “X”)
 */
public interface RomanNumerals {
    String convert(int number);
}
