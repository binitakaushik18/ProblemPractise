package com.gcd;

/**
 * Create a class (GreatestCommonDivisor) with the following interface:
 int calcGCD(int number1, int number2);
 Given two numbers, return the GCD. For those unfamiliar with the algorithm
 * Created by binita on 06/03/15.
 */
public interface CalculateGCD {
    int calcGCD(int number1, int number2);
}
