package com.countdown;

/**
 * Create a class (Countdown) with the following interface:
 int[] countdown (int start);
 Given a starting integers, you must return an array that contains all of the integers from that number to zero (ex. 4 >> [4, 3, 2, 1, 0])
 * Created by binita on 05/03/15.
 */
public interface Countdown {

    int[] countdown (int start);

}
